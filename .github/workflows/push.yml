on: push
name: generate gcode
jobs:
  print:
    name: create gcode from stl
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: slice
      uses: ./.github/workflows/Dockerfile
      env:
        SLICE_CFG: config.ini
      with:
        args: octocat.stl